import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
	title: "AgentPay — Payment Infrastructure for the Agent Economy",
	description:
		"Open-source payment protocol enabling autonomous AI agents to send, receive, escrow, and split SOL payments on Solana with sub-second settlement and sub-cent fees.",
};

export default function RootLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return (
		<html lang="en">
			<body className={inter.className}>
				<Providers>{children}</Providers>
			</body>
		</html>
	);
}
